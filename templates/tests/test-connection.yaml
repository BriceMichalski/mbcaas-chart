apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mbcaas-chart.fullname" . }}-test-connection"
  labels:
    {{- include "mbcaas-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mbcaas-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
